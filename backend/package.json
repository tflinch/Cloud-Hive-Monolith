{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@gradio/client": "^1.19.0",
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "form-data": "^4.0.4",
    "multer": "^2.0.2"
  },
  "scripts": {
    "dev": "nodemon server",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write ./**/*.{js,json} --config ./.prettierrc"
  }
}
